language: c

os: linux
dist: xenial
sudo: required

services: docker

install:
  - docker pull devkitpro/devkitarm

script:
  - docker run -e ENABLE_COMPATIBILITY_REPORTING -v $TRAVIS_BUILD_DIR:/ctrulua devkitpro/devkitarm /bin/bash -ex /ctrulua/.ci/docker.sh
